---
components:
  databases:
    tm:
      name: tm
      domain: "#/components/domains/project-management"
      tables:
        tm_project:
          name: tm_project
          columns:
            - name: id
              type: uuid
            - name: name
              type: varchar(255)
        tm_task:
          name: tm_task
          columns:
            - name: id
              type: uuid
            - name: name
              type: varchar(255)